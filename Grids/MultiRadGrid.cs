using dd_andromeda_poisson_disk_sampling.Propereties;

namespace dd_andromeda_poisson_disk_sampling
{
    public class MultiRadGrid : AbstractMultiRadGrid
    {
        public MultiRadGrid(GridProperties gridProperties, float minRadius, float maxRadius)
            : base(gridProperties, minRadius, maxRadius)
        {
        }
    }
}